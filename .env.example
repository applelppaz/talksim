# Get a Gemini API key from https://aistudio.google.com/apikey
# - Local dev:  copy this file to `.env.local` and fill in the value, then run `vercel dev`.
# - Production: set this variable in your Vercel project (Settings → Environment Variables).
GEMINI_API_KEY=
